stages:
#  - lint
  - test
#  - deploy

variables:
  PYTHON_VERSION: "3.8"

before_script:
  - python3 -m venv venv
  - source venv/bin/activate
  - pip install -r requirements.txt

#lint:
#  stage: lint
#  script:
#    - pip install pylint
#    - pylint my_project  # Replace 'my_project' with your actual project directory or module

test:
  stage: test
  script:
    - pytest --maxfail=1 --disable-warnings -q  # Run tests and stop after the first failure, disable warnings, and be quiet

#deploy:
#  stage: deploy
#  script:
#    - echo "Deploy stage - Customize as needed"  # Add deployment commands if necessary

